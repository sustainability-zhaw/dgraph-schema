mutation {

    addInfoObjectType(input: [
        { name: "Publication" },
        { name: "Project" },
        { name: "Module" }
    ], upsert: true) {
        infoObjectType {
            name
        }
    }
}
