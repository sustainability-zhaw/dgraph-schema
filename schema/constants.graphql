mutation {

    addInfoObjectType(input: [
        { name: "publications" },
        { name: "projects" },
        { name: "modules" }
    ], upsert: true) {
        infoObjectType {
            name
        }
    }
}
