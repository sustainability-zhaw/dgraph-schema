{ 
    types(func: anyofterms(InfoObjectType.name, "publications modules projects")) {
        InfoObjectType.name
        objects: count(InfoObjectType.objects)
    }

    sdg(func: has(Sdg.objects)) {
        Sdg.id
        objects: count(Sdg.objects)
    }

    dept(func: has(Department.objects)) {
        Department.id
        objects: count(Department.objects)
    }
}